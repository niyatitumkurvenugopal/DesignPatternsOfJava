package com.niyati.designpattern.creational.builder;
/**
 * This Builder design pattern is used when we have too many arguments to send in constructors,
 *  and it's hard to maintain the order.
 * And when we don't want to send all parameters in object initialization.
 */