package com.niyati.designpattern.behavioral.memento;

/**
 * Memento Design pattern is used when object state is continuously changing
 * and the user may need to undo the changes.
 * It is also used to restart from last.
 * For example IDE which start restart from changes ,user made before closing the IDE.
 */